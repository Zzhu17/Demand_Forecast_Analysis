Tableau Public requires extracts for publishing.

Use these Hyper extracts in Tableau Public:
- orders_daily.hyper
- forecast_output.hyper

Steps:
1) Open Tableau Public.
2) Connect -> Hyper -> pick `orders_daily.hyper`.
3) Repeat for `forecast_output.hyper` as a second data source.
4) Build dashboards using `dashboard/dashboard_template.md`.
